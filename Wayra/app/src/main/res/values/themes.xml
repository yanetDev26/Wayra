<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme Wayra -->
    <style name="Theme.Wayra" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:navigationBarColor">@color/black</item>
    </style>

    <!-- Splash Screen Theme -->
    <style name="Theme.Wayra.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/black</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.Wayra</item>
    </style>
</resources>
